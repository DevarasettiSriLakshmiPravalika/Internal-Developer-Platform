# Security

Phase 1 contains no authentication, authorization, credentials, or external integrations. Secrets are excluded from source control through `.gitignore`; `.env.example` documents names only.

JWT authentication, password hashing, role-based access, audit logging, and integration credential handling are scheduled for later phases.
